{
    "builds": [{
        "src": "Amadeus_project/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "Amadeus_project/wsgi.py"
        }
    ]
}